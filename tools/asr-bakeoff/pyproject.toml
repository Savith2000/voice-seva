[project]
name = "asr-bakeoff"
version = "0.1.0"
description = "Offline comparison of speech models on Sanskrit chanting"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.4",
    "transformers>=4.44",
    "soundfile>=0.12",
    "numpy>=1.26",
]
